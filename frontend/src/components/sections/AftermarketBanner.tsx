/**
 * AftermarketBanner — REMOVED
 *
 * This component was a duplicate of AftermarketOverview and showed
 * the "Aftermarket Parts" section twice on the landing page.
 *
 * The richer version lives in Service.tsx (AftermarketOverview).
 * Remove <AftermarketBanner /> from app/page.tsx and keep <AftermarketOverview />.
 *
 * This file returns null as a safe fallback until the import is cleaned up.
 */
export default function AftermarketBanner() {
  return null;
}