export const metadata = {
  title: "Support Center | StratumIQ",
  description: "Manage support requests and service conversations from the dashboard.",
};

export default function SupportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
